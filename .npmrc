auto-install-peers=false

# Build-script allow-list lives in ONE place: `onlyBuiltDependencies` in
# pnpm-workspace.yaml (it used to be duplicated here — same keys, different
# spelling — and the copies drifted, silently dropping the @swc/core binary
# on cold installs).