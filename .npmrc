auto-install-peers=false

# pnpm 10 blocks build scripts by default. @nestjs/core's postinstall compiles
# its @swc/core native addon; both must be allow-listed for a cold install.
only-built-dependencies=@nestjs/core,@swc/core