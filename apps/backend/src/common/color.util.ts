/**
 * Accent colors for hierarchy entities (levels, fields, professors, groups,
 * students). Stored as 6-digit hex; the frontend renders them as accent bars.
 */
export const ENTITY_COLOR_PATTERN = /^#[0-9a-fA-F]{6}$/;

export const ENTITY_COLOR_MESSAGE = "Color must be a hex value like #4F46E5";
