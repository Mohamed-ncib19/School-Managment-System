packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks dependency build scripts by default. @nestjs/core needs its
# postinstall on a cold machine, so it stays allow-listed here.
onlyBuiltDependencies:
  - "@nestjs/core"
